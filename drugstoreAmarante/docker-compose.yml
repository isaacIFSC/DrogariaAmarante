version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql-drugstore
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: drugstore
      MYSQL_USER: admin
      MYSQL_PASSWORD: 1234
    ports:
      - "3306:3306"